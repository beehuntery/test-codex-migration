import { Meta, Canvas, Story, Controls } from '@storybook/addon-docs/blocks';
import * as TaskDetailPaneStories from './_components/task-detail-pane.stories';
import * as TaskDeleteButtonStories from './_components/task-delete-button.stories';

<Meta of={TaskDetailPaneStories} title="Tasks/Documentation" />

# Tasks UI 概要

このページは Next.js フェーズ3で構築中のタスク管理 UI コンポーネントを Storybook 上で解説するための MDX ドキュメントです。

## TaskDetailPane コンポーネント

タスク一覧で行を選択した際に表示される右側の詳細ペインです。タイトル・ステータス・期限・タグ・説明を一括で編集し、自動保存（Server Action）で反映されます。

<Canvas>
  <Story of={TaskDetailPaneStories.Default} name="Default" />
</Canvas>

## コントロール

<Controls of={TaskDetailPaneStories.Default} />

## 確認ポイント

- Tailwind トークンによるスタイリングとデザイントークンの適用状況
- タグ編集UIの表示崩れがないか
- 自動保存中のUIブレが発生しないか

## TaskDeleteButton コンポーネント

タスク削除操作を行うためのクライアントコンポーネントです。確認ダイアログを内包しており、Server Action を通じて削除リクエストを発行します。

<Canvas>
  <Story of={TaskDeleteButtonStories.Default} name="Default" />
</Canvas>

「削除」ボタンを押せば、確認ダイアログを表示してフローを理解できます。

<Canvas>
  <Story of={TaskDeleteButtonStories.WithConfirmation} name="Confirm Flow" />
</Canvas>
